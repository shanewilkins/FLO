# Historical Language-Primitive Migration Redirect

This path is retained so existing links do not break.

The migration decisions are recorded in
`docs/design/adr/language_primitive_taxonomy.md`. Current language meaning lives
in `docs/specs/core_language.md`, and current serialized structure lives in
`schema/`.

The one-off migration contract is no longer a permanent policy under the
policy-budget rule in `docs/GOVERNANCE.md`.
