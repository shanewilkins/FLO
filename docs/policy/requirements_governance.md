# Historical Requirements-Governance Redirect

This path is retained so existing links do not break.

The normative role, controlled schemas, immutable-ID rules, lifecycle states,
and change classes for FLO's requirement registers now live in
`docs/GOVERNANCE.md`.

The former CSV schema and `Decision_Needed` workflow were superseded by
`docs/design/adr/governance_v2.md` on 2026-08-09.
