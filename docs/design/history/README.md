# Historical Design Notes

This directory keeps historical context that is useful for background only.

Files here are not normative.
When historical notes disagree with current specs or policy, use current
`docs/specs/`, `docs/policy/`, and `schema/` as authority.

Current historical notes:

- `IR.md`
- `ontology.md`
- `v0_1_architecture_note.md`
