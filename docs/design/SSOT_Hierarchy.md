# FLO Source of Truth Hierarchy (v0.1)

This note is retained for continuity, but the authoritative hierarchy now lives
in `docs/policy/authoritative_artifacts.md`.

## Current rule

- Policy lives in `docs/policy/`.
- Normative artifact definitions live in `docs/specs/`.
- Structural schema contracts live in `schema/`.
- Design notes in `docs/design/` are explanatory unless a policy document says otherwise.

## Practical guidance

Use this file as a pointer, not as the primary authority. New governance or
authority changes should update `docs/policy/authoritative_artifacts.md`.
